# קונטקסט לצ'אט חדש — אתר מיכל לוי

תמשיך איתי על הפרויקט הבא. תקרא את הקובץ הזה במלואו לפני שאתה עושה משהו.

## מה זה
אתר תדמית/פורטפוליו חד-עמודי (עם כמה תת-עמודים) לאמנית ומעצבת הפנים **מיכל לוי**.
עברית מלאה, RTL, בלי בקאנד (`dir="rtl"` בכל מקום).

**תיקיית הפרויקט:** `C:\Users\Callisto\Desktop\אתר מיכל אומנית`
לא רפו git.

## סטאק טכני
- React 18 + Vite 5 + Tailwind CSS 3
- בלי שרת/בקאנד — הכול סטטי
- `src/data/site.js` — מקור האמת היחיד לכל התוכן (טקסטים, פרטי קשר, שירותים, גלריה fallback)
- `src/lib/media.js` — גילוי אוטומטי של מדיה עם `import.meta.glob` מ-`src/media/{hero,gallery,services/<id>}/`. שמים קובץ בתיקייה הנכונה והוא מופיע באתר, בלי לגעת בקוד.
- `src/data/mediaMeta.js` — כיתובים/מטא-דאטה אופציונליים לפי שם קובץ (title, place, category, tall, **price**)
- `src/lib/pricing.js` — לוגיקת תמחור הגלריה (ראה בהמשך)

## מבנה עמודים
- דף בית: Hero (תמונה, לא וידאו — הוסר בעבר), About, Services (5 שירותים), Gallery teaser, Contact
- `/#/gallery` — כל הגלריה עם פילטרים לפי קטגוריה
- `/#/service/<id>` — עמוד לכל שירות (custom, interior, homevisit, workshops, consult)

## טופס יצירת קשר
**וואטסאפ בלבד**, לא אימייל/Web3Forms. `whatsappLink()` ב-`site.js` בונה קישור `wa.me` עם טקסט מוכן מראש (`encodeURIComponent`). מספר הוואטסאפ: `contact.whatsapp = '972505939002'`.

## תכונת תמחור בגלריה (נוספה לאחרונה)
- לכל יצירה בגלריה יש `price` (מחיר בסיס) — ב-`mediaMeta.js` (למדיה אמיתית) וב-`galleryFallback` ב-`site.js` (פלייסהולדרים).
- **כל המחירים מעל ₪2,000** — דרישה מפורשת מהלקוחה.
- המחירים הנוכחיים הם **פלייסהולדר** (מסומן בהערה בקבצים) — צריך להחליף במחירים אמיתיים.
- `src/lib/pricing.js` מגדיר 3 גדלים (`SIZE_OPTIONS`): 50×70, 80×110, 110×150 ס"מ, עם מכפילי מחיר.
- ב-`MediaGrid.jsx` — מחיר "החל מ-" מוצג על כל אריח בגלריה.
- ב-`Lightbox.jsx` (נפתח בלחיצה על תמונה) — בחירת מידה (radio buttons), מחיר מתעדכן לפי הבחירה, וכפתור **"קביעת פגישה"** שפותח וואטסאפ עם הודעה מוכנה שכוללת את שם היצירה, המידה והמחיר.

## ממשק ניהול תוכן (`/#/admin`)
עמוד ניהול פנימי שלא מופיע בתפריט. נכנסים אליו בהוספת `#/admin` לכתובת האתר.
- `src/lib/contentStore.js` — שומר את התוכן הערוך ב-localStorage של הדפדפן. כל ייצוא ב-`site.js` וב-`mediaMeta.js` עובר דרך `editable(key, default)`: אם יש גרסה שמורה היא גוברת, אחרת נלקח הערך מהקובץ.
- **הערכים נקראים פעם אחת בטעינת המודול** — לכן הפאנל עושה `location.reload()` אחרי שמירה. זו כל מכניקת העדכון, ובזכות זה לא היה צריך לשנות אף קומפוננטה.
- `src/pages/AdminPage.jsx` — עורך גנרי אחד (`Field`) שרץ רקורסיבית על מבנה התוכן: טקסט / מספר / צ'קבוקס / רשימה (הוספה, מחיקה, שינוי סדר) / קבוצה. שדה חדש שיתווסף ל-`site.js` יופיע בפאנל אוטומטית בלי עבודה נוספת.
- ה-Header וה-Footer של האתר מוסתרים ב-`/admin` (ראה `Shell` ב-`App.jsx`).
- **חשוב:** השמירה היא לדפדפן הזה בלבד — היא לא מתפרסמת למבקרים באתר. יש כפתורי "ייצוא/ייבוא קובץ" כדי להעביר שינויים ולהפוך אותם לקבועים ב-build הבא.
- מחירי הגלריה האמיתיים יושבים ב-`mediaMeta.js` (לא ב-`galleryFallback`) — שניהם ניתנים לעריכה מהפאנל, בשני מקטעים נפרדים.
- **תמחור לפי מידה:** כל יצירה מחזיקה `sizes: [{label, price}]` (לא מחיר בסיס + נוסחה) — כל מידה ומחירה נערכים בנפרד לגמרי ב-`src/lib/pricing.js`/`mediaMeta.js`. בפאנל אפשר גם להוסיף/למחוק/לסדר מחדש מידות לכל יצירה (מרשימת ה"מידות ומחירים" בתוך הכרטיס שלה).
- **החלפת תמונה מהפאנל:** במקטע "גלריה — תמונות, מידות ומחירים" כל יצירה מציגה תמונה אמיתית עם כפתור העלאה (לחיצה פותחת בחירת קובץ, או לחיצה+הדבקה מהלוח). התמונה נדחסת בדפדפן (`src/lib/imageStore.js`, `fileToCompressedDataUrl` — עד 1400px, JPEG 0.82) ונשמרת מיד (לא ממתינה לכפתור "שמירה") תחת מפתח localStorage נפרד (`michal-site-images-v1`). `withImageOverrides()` ב-`lib/media.js` מיישם את ההחלפה בכל מקום שהגלריה מוצגת (בית/עמוד גלריה/עמודי שירות). "ייצוא קובץ" באדמין כולל גם את התמונות שהוחלפו (JSON גדול יותר), ו"איפוס הכול" מנקה גם אותן.

## פלטת צבעים
פלטה מקורית + 2 גוונים חדשים מ-Pantone moodboard שהלקוחה שלחה:
- `clay` (#A65D35, Potter's Clay 18-1340)
- `warmtaupe` (#A6907E, Warm Taupe 16-1318)
מוגדרים ב-`tailwind.config.js`, כל שימוש נבדק לניגודיות WCAG AA (יש הערה בקוד עם היחסים).

## תמונת "קצת עליי"
`About.jsx` מציג פורטרט אמיתי של מיכל ב-`public/images/michal-portrait.jpg` (עם `object-cover object-top`), עם fallback ל-SVG פלייסהולדר אם התמונה חסרה (`onError`).

## הרצה מקומית
```bash
npm run dev        # localhost:5173 — dev server
npm run build       # בונה ל-dist/
npx vite preview --port 4173 --host   # preview production build
```
ב-`vite.config.js` יש `preview: { allowedHosts: true }` — נדרש כדי שטאנל cloudflared יעבוד (אחרת Vite דוחה Host headers לא מוכרים).

## קישור ציבורי לשיתוף
כשהלקוח מבקש קישור לשלוח לחבר — **לא** דרך שירותי אחסון בתשלום/הרשמה, אלא:
```bash
cloudflared tunnel --url http://localhost:4173 --protocol http2
```
זה נותן קישור `https://<random>.trycloudflare.com` זמני, בלי הרשמה. השרת preview (פורט 4173) חייב לרוץ קודם. **שים לב:** שרת ה-preview נוטה "ליפול" בין הודעות/סשנים — צריך להפעיל אותו מחדש ולוודא עם curl שהוא עונה 200 לפני שפותחים טאנל.

## ⚠️ בעיה חוזרת שצריך להיזהר ממנה
קובץ `src/data/site.js` נפגם כמה פעמים במהלך הפרויקט — שורה בודדת מאבדת תווים (לדוגמה `title: 'קצת עליי',` הפך ל-`titlEm',`). זה גורם לשגיאת build של Vite (`Failed to parse source for import analysis`). כנראה נגרם מעריכה חיצונית/ידנית של הקובץ בו-זמנית. **אחרי כל שינוי בקובץ הזה כדאי להריץ `node --check src/data/site.js`** לפני שמדווחים שהכול עובד.

## סגנון עבודה מול הלקוחה
- הלקוחה (או מי שמעביר לה) שולחת הנחיות בעברית מדוברת, לפעמים עם צילומי מסך מסומנים/מוקפים ביד — צריך להתאים במדויק את הטקסט המבוקש לטקסט הקיים בקוד.
- בכל שינוי טקסט, לבדוק בפועל שהטקסט המתעדכן מופיע נכון (לא להסתמך רק על ה-diff בקוד).
- בסוף כל שינוי גרפי/עיצובי — להריץ preview בדפדפן (Browser pane), לבדוק קונסול נקי, ולבדוק גם moile וגם desktop (אין overflow אופקי).
- כשמתבקש ליפתוח קישור מקומי/ציבורי — לוודא תמיד שהשרת בפועל עונה (curl/HTTP check) לפני שמדווחים שהוא מוכן.
- אם הנחיה לא ברורה/מקוקה (למשל עיגול לא ברור בצילום מסך) — לבחור פרשנות הכי סבירה **ולציין את זה במפורש** ללקוחה כדי שתאשר/תתקן.
